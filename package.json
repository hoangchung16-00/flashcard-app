{
  "name": "flashcard-app",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "yarn workspace @flashcard/shared build && concurrently \"yarn workspace @flashcard/api dev\" \"yarn workspace @flashcard/web dev\"",
    "dev:api": "yarn workspace @flashcard/api dev",
    "dev:web": "yarn workspace @flashcard/web dev",
    "build": "yarn workspace @flashcard/shared build && yarn workspace @flashcard/api build && yarn workspace @flashcard/web build",
    "test": "yarn workspace @flashcard/shared test",
    "db:up": "docker compose up -d",
    "db:down": "docker compose down",
    "db:migrate": "yarn workspace @flashcard/api prisma:migrate",
    "db:seed": "yarn workspace @flashcard/shared build && yarn workspace @flashcard/api prisma:seed"
  },
  "devDependencies": {
    "concurrently": "^9.1.2",
    "typescript": "^5.7.3"
  },
  "dependencies": {
    "firebase": "^12.15.0"
  }
}
